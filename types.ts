import { IconType } from "react-icons";

export type TypeService = {
    title: string;
    about: string;
    Icon: IconType;
};

export type TypeSkill = {
    name: string;
    level: string;
    Icon: IconType;
};
